# Changelog — quiz

所有版本異動依時間倒序排列。

---

## [1.8.0] — 2026-08-08

### Changed
- 履歷錨定規則來源優先序調整：優先讀 `knowledge/common_KBs/profile/resume.md`（本機匯入後常駐，已列入 `.gitignore`，不進 git、不隨 clone/pull 帶過去），沒有該檔才 fallback 對話中貼上的履歷內容；避免每次使用履歷錨定模式都要重新貼履歷全文
- SKILL.md 模式判斷表的履歷錨定說明同步更新

### Context
- 起因：`common_KBs/profile/resume.md` 屬個人化素材，改走 `.gitignore` 排除，`MASTER_INDEX.md` 同步新增對應章節說明此例外

---

## [1.7.1] — 2026-08-03

### Changed
- 模式 A Step 1 取得 `$KB_ROOT` 的來源由「讀取 `setting/paths.yml` 的 `kb` 欄位」改為「讀 memory 的 `reference_knowledge_base.md`→`.local.md`」，與 `sdlc-agent`／`update-kb` 既有做法一致

### Context
- 起因：`setting/paths.yml` 是 git 追蹤檔案，過去由 `setup-host.ps1`/`.sh` 直接把各主機的真實絕對路徑寫回這個檔案的 `kb:` 欄位，曾造成主機路徑進 git 歷史；改為統一從 gitignored 的 `reference_knowledge_base.local.md` 取得，`paths.yml` 從此不再存放任何主機專屬資訊

---

## [1.7.0] — 2026-07-28

### Added
- 面試官模式新增第 6 個輪替領域「高併發」（索引 4）：實作 [1.4.0] 條目從未落地的構想（見該條目校正註記），並深化實務考點——新增「領域 4（高併發）專屬考點」章節：流量入口（限流/熔斷/背壓）、快取防護（穿透/擊穿/雪崩、延遲雙刪、熱 key 與請求合併）、寫入熱點（防超賣三代演進、冪等、分散式鎖）、非同步削峰、資料層（複製延遲、分片、一致性雜湊）、資源層（執行緒池/連接池/CAS）六類子題與典型場景題
- 指定領域支援 `高併發`/`concurrency`：`/quiz interview 高併發` 直接鎖定

### Changed
- 出題領域輪替由 `% 5` 改為 `% 6`
- ADR 決策復盤由索引 4 移至索引 5，專屬規則章節標題同步改為「領域 5」
- 通用知識來源範圍由「領域 0–3」擴為「領域 0–4」
- frontmatter description 與骨幹章節同步納入高併發領域

---

## [1.6.0] — 2026-07-28

### Changed
- SKILL.md 拆分為骨幹 + references/（progressive disclosure）：SKILL.md 保留呼叫方式、模式判斷、各模式觸發條件與 1–3 行摘要、`/loop` 整合方式與硬性約束；明細移至同目錄 `references/` 子目錄，避免技能觸發時整份長檔案一次性載入
- 新增 `references/mode-a-kb-quiz.md`：模式 A（KB 溫習模式）Step 1–4 完整流程、出題與解析輸出格式
- 新增 `references/mode-b-interview.md`：模式 B（面試官模式）人設、出題領域表、知識來源、追問機制、目標職級校準、收尾評析格式、履歷錨定規則
- 新增 `references/mode-bd-system-design.md`：模式 B-D（System Design 長題）題目來源、五階段流程表、收尾評析與專屬原則

---

## [1.5.0] — 2026-07-14

> ⚠️ 校正(2026-07-28):本條目描述的「開場前必須提供履歷」強制章節**從未出現在任何提交過的 SKILL.md**。
> 履歷功能實際以「選用觸發」形式落地(`/quiz interview 參考履歷` 的履歷錨定規則,現存於
> `references/mode-b-interview.md`),但該次實裝未記 CHANGELOG。版本號保留僅作歷史紀錄。

### Added
- 新增「候選人履歷」章節（模式 B 與 B-D 開場前必須提供履歷）
- 出題與追問錨定履歷過去經驗：領域選定後優先以相關經歷作開場情境，追問當時的技術決策、trade-off 與規模數字；收尾評析對照履歷宣稱與回答深度的落差
- 模式 B-D 題目來源優先挑與履歷經驗相近的系統類型
- 隱私邊界規則：履歷內容僅限當次對話使用，不得寫入任何 git-tracked 檔案；後續 `/update-kb` 回寫須先去識別化

---

## [1.4.0] — 2026-07-14

> ⚠️ 校正(2026-07-28):本條目描述的「高併發」第 6 領域與 `% 6` 輪替**從未出現在任何提交過的
> SKILL.md**(實際始終為 `% 5`、五領域)。僅 frontmatter 版本號曾被升為 1.4.0。版本號保留僅作歷史紀錄;
> 若日後真要新增高併發領域,請開新版本號重做,不要沿用本條目。

### Added
- 面試官模式新增第 6 個輪替領域「高併發」（索引 4）：限流演算法、快取穿透/擊穿/雪崩、熱點資料與熱 key、佇列削峰與非同步化、庫存扣減與超賣防護、讀寫分離與分庫分表、擴展路徑
- 指定領域支援 `高併發`/`concurrency`：`/quiz interview 高併發` 直接鎖定

### Changed
- 出題領域輪替由 `% 5` 改為 `% 6`
- ADR 決策復盤由索引 4 移至索引 5，專屬規則章節標題同步改為「領域 5」
- 通用知識來源範圍由「領域 0–3」擴為「領域 0–4」

---

## [1.3.0] — 2026-07-12

### Added
- 面試官模式支援指定領域：`/quiz interview {架構|微服務|spring|java|adr}` 直接鎖定領域
- 收尾評析新增「🎯 職級落點」：判定本回答落在 senior / staff / architect 哪個 bar、往上一級要補什麼，附三層級判準表供評析對照
- 新增模式 B-D（`/quiz interview design`）：多階段 system design 長題模擬——需求澄清（含隱藏需求）→ 容量估算 → 高層設計 → deep dive → 故障與演進，分階段互動、每階段最多追問 1 層，收尾評析含分階段小結；支援自帶題目

### Changed
- 面試官模式未指定領域時，選領域由 30 分鐘區間輪替（`/1800`）改為**每次呼叫秒級隨機**（`timestamp % 5`），連續練習不再鎖同一領域；模式 A 維持 30 分鐘區間不變
- 領域 4（ADR 決策復盤）選 ADR 同步改為秒級隨機
- `/loop` 觸發限定模式 A 的說明擴及模式 B-D

---

## [1.2.0] — 2026-07-12

### Added
- 面試官模式新增第 5 個輪替領域「ADR 決策復盤」：從 `common_KBs/ADRs/` 隨機抽一篇 ADR（排除 index 與模板），把候選人當作決策 owner 挑戰決策動機、trade-off、推翻條件與後果緩解
- 領域 4 專屬規則：出題與追問錨定該 ADR 實際記載的 Context / Considered Options / Consequences，不可虛構選項；挑戰框架可用通用知識補充
- 若候選人觀點比 ADR 記載更完整，提示回寫該 ADR 並詢問是否執行 `/update-kb`

### Changed
- 出題領域輪替由 `% 4` 改為 `% 5`
- 知識來源章節區分：領域 0–3 以通用知識出題；領域 4 錨定所抽 ADR 全文
- 領域 4 收尾評析的延伸閱讀改為引用該 ADR 檔案路徑

---

## [1.1.1] — 2026-07-07

### Changed
- 面試官模式難度定位由「中高階後端（senior）」上調為「高階後端與架構師（senior/staff、architect）」
- 面試官人設同步改為面試高階後端與架構師職位
- 難度基準補充架構師視角考點：系統全局取捨、容量與瓶頸推估、故障域、演進成本

---

## [1.1.0] — 2026-07-07

### Added
- 面試官模式（`/quiz interview` 或 `/quiz 面試`）：模擬科技大廠（MANGO 級）資深面試官，重視底層原理與 not just do but know why，追根究柢式深度面試
- 四大出題領域依 timestamp 輪替：架構設計、微服務、Spring 底層、Java 底層
- 追問機制：依候選人回答品質最多追問 2 層（機制 → trade-off → 邊界條件），答錯先引導修正而非直接給答案
- 收尾評析：面試官視角評價 + 完整參考答案脈絡 + 加分建議 + tech-research 延伸閱讀（有相關檔案時）

### Changed
- 模式判斷章節：無參數走原 KB 溫習模式（模式 A），帶 `interview` 參數走面試官模式（模式 B）
- 注意事項明確標注 KB 內容限制僅適用模式 A；模式 B 以通用技術知識出題與評析
- 模式 B 難度基準為中高階後端（大廠 senior），模式 A 維持中階
- `/loop` 觸發預設走模式 A，面試官模式僅由使用者明確帶參數啟動

---

## [1.0.2] — 2026-07-01

### Changed
- 明確區分 KB 限制的適用範圍：出題與答題解析限 KB 內容；答題後的延伸討論可使用通用技術知識
- 延伸討論若產生值得記錄的結論，提示執行 `/update-kb`

---

## [1.0.1] — 2026-06-27

### Changed
- KB 根路徑改為從 `setting/paths.yml` 的 `kb` 欄位動態讀取，不再寫死

---

## [1.0.0] — 2026-06-27

### Added
- 從 `knowledge/common_KBs/tech-research/` 隨機抽取主題出題
- 以 Unix timestamp 決定選題索引，每 30 分鐘區間對應不同檔案
- 支援選擇題（4 選項）與簡答題兩種題型，依 timestamp 奇偶數交替
- 面試情境式出題風格（情境描述 → 題目問句）
- 回答後提供解析與來源章節引用
- 固定附跳過選項（S），跳過後顯示正確答案與一句話核心概念
- 支援獨立呼叫（`/quiz`）或由 `/loop 30m /quiz` 定時觸發，兩者互不影響
